<p id="notice"><%= notice %></p>

<h1>Audits realizados</h1>

<table>
  <thead>
    <tr>
      <th>Preg 1  </th>
      <th>Preg 2  </th>
      <th>Preg 3  </th>
      <th>Total</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.i1 %></td>
        <td><%= item.i2 %></td>
        <td><%= item.i3 %></td>
        <td><%= item.total %></td>
       <!--  <td><%= link_to 'Mostrar', item %></td>
        <td><%= link_to 'Editar', edit_item_path(item) %></td> -->
        <td><%= link_to 'Borrar', item, method: :delete, data: { confirm: 'Estás seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Hacer Audit', new_item_path %>
